from ..utils import _object


class Block_(_object.McnpElement_):
    """
    Represents generic PTRAC blocks.
    """

    def to_mcnp(self):
        """
        Generates PTRAC from ``Block_``.

        Returns:
            INP for ``Block_``.
        """

        assert False, "I'm working on it!"
