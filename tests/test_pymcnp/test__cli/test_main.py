import subprocess


class Test_Main:
    class Test_Main:
        def test(self):
            subprocess.run(['pymcnp', 'help', 'check'])
            subprocess.run(['pymcnp', 'help', 'convert'])
            subprocess.run(['pymcnp', 'help', 'plot'])
            subprocess.run(['pymcnp', 'help', 'run'])
            subprocess.run(['pymcnp', 'help', 'visualize'])
            subprocess.run(['pymcnp', 'help'])
            subprocess.run(['pymcnp', 'help', 'hello'])
